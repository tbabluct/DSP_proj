function [outputArray] = boolSmooth(inputArray,window)
%BOOLSMOOTH Summary of this function goes here
%  Takes input boolean array and the smoothing window. If the input signal
%  is high within the search window around a point, the output signal is
%  high. Intended to smooth DTFM boolean presence signals.
k = length(inputArray);
outputArray = zeros(1,k+ 2*window);
for i =window: k+window
    if

end

